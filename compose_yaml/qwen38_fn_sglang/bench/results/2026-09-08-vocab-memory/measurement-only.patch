--- eagle_worker_v2.py
+++ eagle_worker_v2.py
@@ -1,4 +1,5 @@
 import contextlib
+import json
 import logging
 import time
 from dataclasses import replace
@@ -289,6 +290,9 @@
             self.hot_token_id = None
 
     def init_lm_head(self):
+        torch.cuda.synchronize()
+        _vocab_before = {"allocated_bytes": torch.cuda.memory_allocated(), "reserved_bytes": torch.cuda.memory_reserved(), "prior_peak_allocated_bytes": torch.cuda.max_memory_allocated()}
+        torch.cuda.reset_peak_memory_stats()
         embed, head = self.target_worker.model_runner.model.get_embed_and_head()
         target_lm_head = getattr(self.target_worker.model_runner.model, "lm_head", None)
 
@@ -328,6 +332,11 @@
             # Share the embedding and lm_head
             self.draft_runner.model.set_embed_and_head(embed, head)
             maybe_share_target_lm_head()
+
+        torch.cuda.synchronize()
+        _vocab_draft_head = self.draft_runner.model.lm_head.weight
+        _vocab_record = {"before": _vocab_before, "after_allocated_bytes": torch.cuda.memory_allocated(), "after_reserved_bytes": torch.cuda.memory_reserved(), "init_peak_allocated_bytes": torch.cuda.max_memory_allocated(), "draft_head_shape": list(_vocab_draft_head.shape), "draft_head_dtype": str(_vocab_draft_head.dtype), "draft_head_bytes": _vocab_draft_head.numel() * _vocab_draft_head.element_size(), "shares_target_head": _vocab_draft_head.data_ptr() == target_lm_head.weight.data_ptr(), "shortlist_size": len(self.hot_token_id) if self.hot_token_id is not None else None}
+        logger.info("VOCAB_MEMORY_HEAD %s", json.dumps(_vocab_record, sort_keys=True))
 
     def init_attention_backend(self):
         # Create multi-step attn backends and cuda graph runners

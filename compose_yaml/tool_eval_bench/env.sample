# Empty URL/model: discover the running local model. CLI flags override these.
TOOL_EVAL_BASE_URL=
TOOL_EVAL_MODEL=
TOOL_EVAL_API_KEY=
# Optional local Hugging Face cache/model directory, mounted read-only at /models.
BENCH_MODEL_DIR=${HOME}/.cache/huggingface

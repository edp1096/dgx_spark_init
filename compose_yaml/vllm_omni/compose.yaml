services:
  runtime-check:
    build:
      context: .
    image: dgx-vllm-omni:v0.26.0
    profiles: [check]
    command: ["python3", "-c", "import torch, vllm_omni; print(torch.cuda.get_device_name(0))"]
    gpus: all
    ipc: host

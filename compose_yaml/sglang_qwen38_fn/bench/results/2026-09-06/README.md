# 2026-09-06 TP1 비교 — 잠정 결과

후속 [원본을 보존한 3회 재시험](../2026-09-06-recheck/README.md)을 우선 참고한다.

기존 dealignai Qwen3.8-Flash-Next ABLITERATED NVFP4 가중치와 SGLang을 유지하고,
초안 모델에만 한국어 포함 65,536개 어휘 목록을 적용했다. TP=1, context=65536,
mem_fraction_static=0.79, prefill chunk=1024, MTP steps=3/draft tokens=4,
KV dtype=auto, 동시 요청 상한=2. 최종 검증 모델의 어휘와 가중치는 그대로다.

각 항목 2회, temperature=0, thinking=off, 요청 전 prefix cache 제거.
아래 수치는 재부팅 전 완료된 시험에서 확인한 decode tok/s 중앙값이다.

| 항목 | 기존 | 한국어 64K | 변화 |
|---|---:|---:|---:|
| 한국어 설명 | 28.51 | 32.38 | +13.6% |
| 한국어 계획 | 31.16 | 34.96 | +12.2% |
| 한국어 이야기 | 21.73 | 19.87 | -8.5% |
| 한국어 비교 | 31.00 | 27.29 | -12.0% |
| Python | 29.11 | 41.18 | +41.4% |
| Go | 32.34 | 37.02 | +14.5% |
| JSON | 34.69 | 40.09 | +15.6% |
| 영어 설명 | 21.72 | 25.26 | +16.3% |

출력 토큰 합계 / 요청 시간 합계는 26.89 → 30.10 tok/s (약 +11.9%).
두 경우 모두 thinking ON/OFF 도구 호출, 이미지, 동시 요청 2개,
35,044토큰 문맥에서 키 검색, JSON 형식 등 7개 기능 검사를 통과했다.
측정 구간의 최소 시스템 가용 메모리는 기존 22.38 GiB, 후보 21.25 GiB였다.

## 자료와 한계

- baseline.json, baseline-config.json, baseline-server.json, corpus.json은 보존된 원본이다.
- 후보 결과 원본은 /tmp에 있었고 16:53:31 KST 호스트 재부팅으로 소실됐다.
  위 후보 수치는 재부팅 전에 확인한 요약을 기록한 것으로 원본 JSON을 복원한 것이 아니다.
- 시험 종료 뒤 후보 컨테이너가 올라간 상태에서 비정상 재부팅이 발생했다.
  남은 로그에서 OOM/Xid나 정상 종료 기록은 확인되지 않았다. 원인은 미확정이다.
  측정 구간의 메모리 여유만으로 안정성이 증명되지는 않는다.
- 2회 표본이며 기존 설정 자체의 편차도 크다. 응답 내용도 달라질 수 있다.
  통계적 성능 보장, 한국어 품질 동등성, 장시간 안정성 검증을 의미하지 않는다.

따라서 기본값은 전체 어휘(off)로 유지하고 ko64k는 실험 옵션으로 제공한다.
재시험 결과는 /tmp 대신 영구 경로에 저장해야 한다.
